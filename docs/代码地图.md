# grok-md-reader 代码地图

> 给「下一个 Claude 会话」用的**代码层导航**——只装**稳定、不常变**的东西(模块/目录划分、加新东西放哪、构建/测试/运行入口)。
> **易变的别写这里**(会腐):当前进度/下一步看 `vitals proj now grok-md-reader --pretty`;改了什么看 `git log --oneline`(若用 git);为什么这么设计看 `docs/decisions/`(ADR)。
> **首次安装时几乎为空,随项目长出结构再逐步填**——保持简短。**绝不堆自动生成的文件树/接口清单**(那是噪音,会随代码腐烂;这也是本 skill 早期版本砍掉 architecture 文档的教训)。

## 模块 / 目录划分

<!-- 各主要模块/目录一句职责,只列稳定的骨架。例:
| 模块/目录 | 职责 | 关键文件 |
|---|---|---|
| `src/core/` | 核心逻辑,不含 UI | ... |
| `src/cli/`  | 命令行入口 | ... |
-->
(待填)

## 加新东西放哪(约定)

<!-- 把「新增一个 X 该放哪个文件、怎么接进去」写成清单,让新会话不用猜、不乱放。例:
- 新增一个命令 → `<文件>`,并在 `<注册处>` 注册
- 新增一张表/迁移 → `<目录>`,只增不改历史
- 新增一个页面/接口/模块 → `<位置>`,怎么接
- 测试放哪、怎么跑 → `<目录>` / `<命令>`
能用测试/lint 卡住的约定,优先写成测试(机器强制 > 靠人记)。
-->
(待填)

## 构建 / 测试 / 运行入口

<!-- 例:
| 动作 | 命令 |
|---|---|
| 构建 | ... |
| 测试 | ... |
| 运行 / 部署 | ... |
-->
(待填)
