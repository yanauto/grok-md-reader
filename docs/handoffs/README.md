# handoffs · 交接冷区

存放：给同事的交接说明、某日 handoff 原文、对外交付叙事。

## 规则

1. **保存不删**（冷证据）  
2. **默认不读**——不进开场必读；AI 不主动通读本目录  
3. **提炼义务**：文中「以后还要用」的接口/步骤/命令 → **同时**写入 `docs/工程手册.md` 或 `docs/playbooks/`；交接里写「权威见手册 §x」  
4. 过期包/重复草稿 → `docs/_archive/`

考古时再 grep / 打开具体文件。
